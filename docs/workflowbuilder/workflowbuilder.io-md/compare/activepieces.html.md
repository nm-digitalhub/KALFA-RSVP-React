# ActivePieces Alternative: React SDK for Embed in SaaS

Target audienceOps teams, SMB, Zapier replacement seekersProduct engineering teams embedding a canvas in their SaaSPrimary embed surfaceIframe of ActivePieces UI / white-label hosted instanceReact component (canvas inside your UI)Editor as React SDKNo - canvas is internal to the ActivePieces app, not packaged as an importable component; embed = iframeYes. Native React component, theme-awareFrontend frameworkReact 18 + Vite + XYFlow (React Flow) + Tailwind + shadcn + Zustand + TanStack QueryReact + TypeScriptOpen-source licenseMIT on the core; Embed license needed for white-label resaleApache 2.0, no restrictionsWhite-label and resaleIn-house allowed; resale requires Embed license contractAllowed by licenseSelf-hosted optionYes - MIT, Docker / Kubernetes / Helm / one-clickYes - Apache 2.0, your infraUX ownershipActivePieces UI inside iframe; pieces extensible via SDK, canvas extension requires forking the appYou own canvas, nodes, theme, routingCustom node typesTypeScript Pieces SDK, CLI scaffoldTypeScript node API, full form schema controlCustom canvas extensionBoth projects render on XYFlow / React Flow, but ActivePieces does not publish the canvas as a consumable SDK - extension means forking the appYes - React Flow exposed via plugin API, you can extendGeneric workflow shapesiPaaS shape - trigger + action chainsFirst-class - data, approval, back-office, domain nodesAI and agent supportYes - AI Agent builder with the full piece catalog (~280+) plus MCP servers as toolsYes, runtime-agnostic (LangGraph, Temporal, custom)Pre-built integrations280+ pre-built pieces + separate MCP server catalog (massive head start)Pattern library + reference nodes; bring your ownRuntime and execution engineActivePieces engine baked in (TypeScript backend)Bring your own (Temporal adapter ships in reference backend)Time to first integration~1-2 hours for non-coder flow on hosted ActivePieces; embedding inside your product = iframe of the AP app (no canvas SDK to mount)~1 day for white-label embed (Athena Intelligence case)Backend stackTypeScript (Node.js)Your choice - Node, Go, Python, .NET, anyPricing - cloud entryFree / $25 Plus / $150 Business / Enterprise customn/a (SDK, not hosted product)Pricing - perpetual licenseEnterprise Embed license is recurringEUR 6,990 one-time EnterpriseExpert supportCommunity + commercial Enterprise plan15+ years of diagramming, 200+ commercial projects delivered