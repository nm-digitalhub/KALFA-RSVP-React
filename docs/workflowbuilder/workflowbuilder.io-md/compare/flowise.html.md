# Flowise Alternative: Embeddable Workflow Editor SDK

Target audienceAI builders deploying chatbots and agent flowsProduct engineering teams embedding a canvas in their SaaSPrimary embed surfaceChat widget (flowise-fullchatbot and bubble variant) and REST APIReact component (canvas inside your UI)Editor as React SDKNo. React frontend exists but is not packaged as an SDKYes. Native React component, theme-awareOpen-source licenseOpen-core: Community Edition Apache 2.0; enterprise/ directory under separate Commercial LicenseApache 2.0, no separate enterprise tierWhite-label and resaleCommunity Edition allows white-label and resale; enterprise features (SSO, RBAC, IdentityManager) require paid licenseAllowed without restrictionsSelf-hosted optionYes. Community Edition free under Apache 2.0; paid Enterprise license for SSO, RBAC, audit logs, air-gapped deploymentYes. Apache 2.0, your infraUX ownershipEditor UI is Flowise-branded; node types extensible, canvas is notYou own canvas, nodes, theme, routingCustom node typesTypeScript node SDK, well-structured component modelTypeScript node API, full form schema controlCustom canvas extensionNo. React Flow is internalYes. React Flow is exposed, you can extendGeneric workflow (non-AI)Scoped to LLM and agent flowsFirst-class. Data, approval, back-office, domain nodesAI and agent supportYes, native to LangChain.jsYes, runtime-agnostic (LangGraph, Temporal, custom)Multi-channel deploy (Telegram, WhatsApp)Not native. Integrate via REST API + your own bot or bridgeBring your own channel adapterPre-built LLM nodesLarge pre-built library on LangChain.jsPattern library + reference nodes; bring your ownRuntime and execution engineLangChain.js runtime baked inBring your own (Temporal adapter ships in reference backend)Time to first integration~1-2 hours for chatbot widget; canvas embed not supported~1 day for white-label embed (Athena Intelligence case)Frontend stackReact + LangChain.js (React Flow internal)React + TypeScriptBackend stackNode.js + LangChain.jsYour choice. Node, Go, Python, .NET, anyPricing - cloud entry$35/mo Starter, $65/mo Pro, custom Enterprisen/a (SDK, not hosted product)Pricing - perpetual licenseEnterprise license is recurringEUR 6,990 one-time EnterpriseExpert supportCommunity + paid Enterprise plan15+ years of diagramming, 200+ commercial projects delivered